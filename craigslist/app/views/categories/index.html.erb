<h1>Craigslist Jr.</h1>

<table>
  <tr>
    <th>Category Title |</th>
    <th>Article Count</th>
  </tr>

  <% @categories.each do |category| %>

  <tr>
    <th><%= link_to category.name, category_path(category) %></th>
    <th><%= category.articles.count %></th>
  </tr>

  <% end %>
</table>
